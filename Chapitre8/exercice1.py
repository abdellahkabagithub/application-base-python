#importation du module de la fonction sqrt
from math import sqrt
from math import cos
import math
for i in range(10,21) :
     racine = sqrt(i)
     print(round(racine,3),end=',')

print("\n")
#Calcul du cosinus

x=cos(math.pi/2)
print(f"le cosinus de x est {x}")